import { AuthProps } from "react-admin";
import { RouteChildrenProps } from "react-router";

// export function authProps(props: Partial<AuthProps>): AuthProps {
//     const { hasCreate, hasEdit, hasShow, hasList } = props;
//     return {
//         hasCreate: Boolean(hasCreate),
//         hasEdit: Boolean(hasEdit),
//         hasShow: Boolean(hasShow),
//         hasList: Boolean(hasList)
//     };
// }

// interface AdminRouteProps {
//     id?: string;
// }

// interface WithStaticContext {
//     staticContext: undefined;
// }

// export type AdminArgs = WithStaticContext & RouteChildrenProps<AdminRouteProps>;
